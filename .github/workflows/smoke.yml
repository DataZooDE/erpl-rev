name: Smoke test (published bundles)

# Platform smoke test, totally independent of the build.
#
# Mirrors the erpl / erpl-web "can the extension load on every platform?" check:
# it downloads the SHIPPED single-file bundle for each OS and runs it on a clean
# runner — no build toolchain, no vcpkg, no SAP NW RFC SDK or DuckDB on disk — to
# prove the C++ binary starts everywhere and the SAP libraries bundled next to it
# actually load and are callable (`--smoke` calls RfcGetVersion + a DuckDB query,
# which initialises the SAP SDK incl. its ICU dependency; no SAP gateway needed).
#
# Runs automatically after the "Release bundles" build completes (workflow_run
# fires for tag-published releases even though they are created by GITHUB_TOKEN,
# which a `release: published` trigger would NOT — GitHub suppresses recursive
# workflow triggering for token-created releases), and on demand against any tag.
# Never part of the build itself.
on:
  workflow_run:
    workflows: ["Release bundles"]
    types: [completed]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to smoke-test (blank = latest release)'
        required: false
        default: ''

permissions:
  contents: read       # gh release download

jobs:
  resolve:
    name: Resolve release tag
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.t.outputs.tag }}
    steps:
      - id: t
        env:
          GH_TOKEN: ${{ github.token }}
          INPUT_TAG: ${{ inputs.tag }}
          # On workflow_run, head_branch is the ref that triggered the release
          # build — the tag name (e.g. v2026.06.11) for a tag push, else "main".
          RUN_BRANCH: ${{ github.event.workflow_run.head_branch }}
          RUN_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            # Only validate a successful release build that built a version tag.
            if [ "$RUN_CONCLUSION" != "success" ]; then
              echo "Release build concluded '$RUN_CONCLUSION' (not success); skipping."
              echo "tag=" >> "$GITHUB_OUTPUT"; exit 0
            fi
            case "$RUN_BRANCH" in
              v*) TAG="$RUN_BRANCH" ;;
              *)  echo "Release build ref '$RUN_BRANCH' is not a version tag; skipping."
                  echo "tag=" >> "$GITHUB_OUTPUT"; exit 0 ;;
            esac
          else
            TAG="$INPUT_TAG"
            if [ -z "$TAG" ]; then
              TAG=$(gh release view --repo "$GITHUB_REPOSITORY" --json tagName -q .tagName)
            fi
          fi
          echo "Smoke-testing release tag: $TAG"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

  smoke:
    name: smoke ${{ matrix.bin }}
    needs: resolve
    if: needs.resolve.outputs.tag != ''
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest,  archive: erpl-rev-linux-amd64.tar.gz,  bin: erpl-rev-linux-amd64 }
          - { os: macos-14,       archive: erpl-rev-macos-arm64.tar.gz,  bin: erpl-rev-macos-arm64 }
          - { os: windows-latest, archive: erpl-rev-windows-amd64.zip,   bin: erpl-rev-windows-amd64.exe }
    steps:
      - name: Smoke-test the published bundle (posix)
        if: runner.os != 'Windows'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          set -euo pipefail
          gh release download "$TAG" --repo "$GITHUB_REPOSITORY" -p "${{ matrix.archive }}" --clobber
          tar -xzf "${{ matrix.archive }}"
          chmod +x "${{ matrix.bin }}"
          echo "::group::./${{ matrix.bin }} --smoke (no LD_LIBRARY_PATH / DYLD_LIBRARY_PATH)"
          # Strip any inherited loader paths: the bundle must resolve DuckDB
          # entirely from its own self-extracted directory, and must not be
          # quietly picking up an RFC library from the runner.
          out=$(env -u LD_LIBRARY_PATH -u DYLD_LIBRARY_PATH "./${{ matrix.bin }}" --smoke)
          echo "$out"
          echo "::endgroup::"
          echo "$out" | grep -q "smoke ok" || { echo "FAIL: no 'smoke ok' line"; exit 1; }
          # The backend, not the SDK. Since v2026.08.30 the bundles talk RFC
          # through erpl-proto, linked statically, and carry no SAP NW RFC SDK at
          # all -- so the old "SAP NW RFC SDK" assertion had been failing every
          # smoke run on all three platforms since that release. It ran after
          # publication, gated nothing, and so went unread for six releases.
          echo "$out" | grep -q "RFC backend erpl-proto" \
            || { echo "FAIL: RFC backend is not erpl-proto"; exit 1; }
          # And prove the negative the bundle actually promises.
          echo "$out" | grep -q "SAP NW RFC SDK" \
            && { echo "FAIL: bundle reports an SAP NW RFC SDK; it must carry none"; exit 1; }
          true

      - name: Smoke-test the published bundle (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          $ErrorActionPreference = 'Stop'
          gh release download $env:TAG --repo $env:GITHUB_REPOSITORY -p "${{ matrix.archive }}" --clobber
          Expand-Archive -Force "${{ matrix.archive }}" -DestinationPath .
          $out = & ".\${{ matrix.bin }}" --smoke 2>&1 | Out-String
          Write-Host $out
          if ($LASTEXITCODE -ne 0)       { Write-Error "smoke exited $LASTEXITCODE"; exit 1 }
          if ($out -notmatch 'smoke ok') { Write-Error "no 'smoke ok' line"; exit 1 }
          # See the comment on the unix step: erpl-proto, and no SDK.
          if ($out -notmatch 'RFC backend erpl-proto') {
            Write-Error "RFC backend is not erpl-proto"; exit 1
          }
          if ($out -match 'SAP NW RFC SDK') {
            Write-Error "bundle reports an SAP NW RFC SDK; it must carry none"; exit 1
          }
